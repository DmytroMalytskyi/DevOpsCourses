<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome to Nginx!</title>
  <style>
    body { 
      font-family: Arial, sans-serif; 
      background-color: #f0f8ff; 
      color: #333; 
      text-align: center; 
      padding: 50px; 
    }
    h1 { color: #007acc; }
    p { font-size: 1.2em; }
    .container { 
      border: 2px solid #007acc; 
      padding: 20px; 
      border-radius: 10px; 
      display: inline-block; 
      background-color: #ffffff; 
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Welcome to Nginx!</h1>
    <p>This page is served via Docker Compose on Ubuntu 22.04.</p>
    <p>Server IP: {{ ansible_default_ipv4.address }}</p>
    <p>Accessed at: {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
  </div>
</body>
</html>
